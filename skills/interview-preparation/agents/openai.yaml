interface:
  display_name: "Interview Preparation"
  short_description: "Build a round-specific plan from shared evidence"
  default_prompt: "Use $interview-preparation to prepare the exact target round from shared application evidence and selected prior-round handoffs."
policy:
  allow_implicit_invocation: true
