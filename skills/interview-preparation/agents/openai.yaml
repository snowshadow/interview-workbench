interface:
  display_name: "Interview Preparation"
  short_description: "Build a sharp candidate-specific interview plan"
  default_prompt: "Use $interview-preparation to prepare this interview and save the plan to the workbench."
policy:
  allow_implicit_invocation: true
