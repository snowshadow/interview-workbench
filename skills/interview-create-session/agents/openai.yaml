interface:
  display_name: "Create Interview Application or Round"
  short_description: "Create an application or add an interview round"
  default_prompt: "Use $interview-create-session to create the exact candidate-and-role application or add the requested round in Interview Workbench."
policy:
  allow_implicit_invocation: true
